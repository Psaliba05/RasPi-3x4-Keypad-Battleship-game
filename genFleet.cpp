#include "genFleet.h"
#include <iostream>
#include <string>

/* int main() {
    std::string myMap[GRID_SIZE][GRID_SIZE];
    // Define the fleet sizes.
    const unsigned short fleet[FLEET_COUNT] = {5, 4, 3, 3, 2, 2, 2};

    resetFleet(myMap);
    autogenFleet(myMap, fleet);
    
    std::cout << "Simplified Fleet Board:" << std::endl;
    printFleet(myMap);
    
    std::cout << "\nDetailed Fleet Board:" << std::endl;
    printFleetDetailed(myMap);

    return 0;
} */
